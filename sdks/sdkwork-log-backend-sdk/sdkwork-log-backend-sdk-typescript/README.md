# sdkwork-log-backend-sdk

Minimal SDK workspace scaffold created by `sdkgen init`.

## Scaffold

- Language: typescript
- SDK type: backend
- Version: 0.1.0
- Package name: @sdkwork/log-backend-sdk

## Next

1. Prepare an OpenAPI 3.x specification.
2. Run `sdkgen generate -i ./openapi.json -o . -n sdkwork-log-backend-sdk -l typescript -t backend --package-name @sdkwork/log-backend-sdk --description "SDKWork Log backend API SDK"`.
3. Keep hand-written extensions in `custom/` so regeneration stays safe.
4. Run `sdkgen inspect -o .` after generation when you need a machine-readable generated SDK evidence snapshot.
