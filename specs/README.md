# sdkwork-log specs

Module-local spec system for the SDKWork log foundation module. Machine authority: `component.spec.json`.

- `component.spec.json` — module identity, contracts, verification commands.
- Global standards are referenced by relative path (`../../sdkwork-specs/*_SPEC.md`); never copied locally.
